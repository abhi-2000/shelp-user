<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.retrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ParticularCategoryActivity"></activity>
        <activity android:name=".ResetotpConfirm" />
        <activity android:name=".resetotp" />
        <activity android:name=".UI.CourseDetail" />
        <activity android:name=".UI.student" />
        <activity android:name=".UI.SignupActivity" />
        <activity android:name=".UI.otp" />
        <activity android:name=".UI.loginActivity" />
        <activity android:name=".resetPassword" />
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>